<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="FullText">
            <directory>tests/FullText</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Tests run against their own MySQL schema, never the database in
             .env: tests/FullText uses DatabaseTruncation, which empties every
             table it touches. Pointing the suite at the development database
             would wipe it. Create it once with:
                 CREATE DATABASE rbeverything_testing
                     CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
             The name matches the one CI provisions in deploy.yml. -->
        <env name="DB_DATABASE" value="rbeverything_testing"/>
        <env name="MAIL_MAILER" value="array"/>
        <!-- Off by default so every article-creating test does not rewrite
             public/sitemap.xml; SeoSitemapTest enables it explicitly. -->
        <env name="AUTO_SITEMAP" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
